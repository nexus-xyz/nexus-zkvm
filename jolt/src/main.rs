fn main() {
    // let path = "";
    // let bytes = std::fs::read(&path).unwrap();
    // let vm = nexus_jolt::parse::parse_elf(&bytes).unwrap();

    // let preprocess = nexus_jolt::preprocess(&vm);
    // let (proof, commitments) = nexus_jolt::prove(vm, &preprocess).unwrap();

    // let _ = nexus_jolt::verify(preprocess, proof, commitments).unwrap();
    // prove(path).unwrap();

    // println!("Hello, world!");
}
